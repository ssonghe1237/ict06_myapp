<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="spring.ict06team1.midpj.dao.TestDAO">  <!-- namespace="패키지명.인터페이스" -->
	<!-- 1-1. user정보, address 정보 -->
	<select id="getAddressInfo_1" resultMap="userMap1">
		SELECT u.user_id, 
		       u.user_name, 
		       u.reg_date,  
		       a.user_address
		  FROM mvc_user_tbl u
		     , mvc_address_tbl a
		 WHERE u.user_id = a.user_id
		 ORDER BY u.user_id ASC	
	 </select>
	 
	 <!-- 1-2. 매핑 -->
	 <resultMap id="userMap1" type="spring.ict06team1.midpj.dto.TestUserDTO">
	 	<id property="userId" 		column="user_id"/> <!-- pk -->
	 	<result property="userName" column="user_name"/>
	 	<result property="regDate" 	column="reg_date"/>
	 	<association property="addressDTO" column="user_id" resultMap="getAddress"/> <!-- fk, Association 관계 -->
	 </resultMap>
	 
	 <!-- 1-3. address 정보 -->
	 <resultMap id="getAddress" type="spring.ict06team1.midpj.dto.TestAddressDTO">
	 	<result property="userAddress" column="user_address"/>
	 </resultMap>
 </mapper> 